<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="FreeFlow_App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FreeFlow_App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="FreeFlow for Windows" Style="{StaticResource TitleLargeTextBlockStyle}" />
            <TextBlock
                Text="Phase 4 preview — pipeline + settings wired against mock platform services."
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <Button
                x:Name="RunButton"
                Click="OnRunDictationClick"
                Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE720;" FontFamily="Segoe MDL2 Assets" />
                    <TextBlock Text="Run mock dictation" />
                </StackPanel>
            </Button>
            <Button x:Name="SettingsButton" Click="OnSettingsClick">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE713;" FontFamily="Segoe MDL2 Assets" />
                    <TextBlock Text="Settings" />
                </StackPanel>
            </Button>
        </StackPanel>

        <InfoBar
            Grid.Row="2"
            x:Name="StatusBar"
            IsOpen="False"
            IsClosable="False" />

        <Border
            Grid.Row="3"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
            <ScrollViewer>
                <StackPanel Spacing="12">
                    <StackPanel Spacing="2">
                        <TextBlock Text="Focused app" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="ContextText" Text="—" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Spacing="2">
                        <TextBlock Text="Raw transcript" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="RawText" Text="—" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Spacing="2">
                        <TextBlock Text="Cleaned (pasted) text" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="CleanedText" Text="—" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="Wrap" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
